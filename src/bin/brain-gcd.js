#!/usr/bin/env node
import { welcome, askName } from '..';

welcome();
const name = askName();
const firstNum = makeRandomNumber(1, 10); // random from 1 to 10 to make game more simpliestconst secondNum = makeRandomNumber(1, 10); // random from 1 to 10 to make game more simpliest
const secondNum = makeRandomNumber(1, 10); // random from 1 to 10 to make game more simpliestconst secondNum = makeRandomNumber(1, 10); // random from 1 to 10 to make game more simpliest

